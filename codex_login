# CODEX LOGIN (MASTER BOOTSTRAP + SOT)

Use this file at the start of every new conversation.
If user references `@codex_login`, follow this contract strictly.

## 1) Role Contract (Mandatory)
- Codex role: `DIRECTOR / MONITOR / AUDITOR` (architecture, planning, review, verification).
- Antigravity role: `IMPLEMENTATION TEAM` (Gemini Pro, Gemini Flash, Claude Sonnet, Claude Ops, etc.).
- Codex must NOT implement product code unless user explicitly writes:
  - `CODEX_OVERRIDE_IMPLEMENT`

## 2) Work Sequence (Always)
1. Understand requirement in simple words.
2. Make detailed page-wise plan.
3. Send plan for Claude Sonnet review.
4. Update plan with findings.
5. Assign task-by-task execution to Antigravity.
6. Audit implementation (logic, SOT parity, tests, regressions).
7. Approve or reject with exact actions.

## 3) Commit / Push Gates
- Antigravity can commit only after Codex status:
  - `COMMIT_ALLOWED`
- Antigravity can push only after Codex status:
  - `PUSH_ALLOWED`
- Without these statuses, no commit/push.

## 4) Dual-Agent Safety Guards
Set agent identity in shell before work:
- `export AGENT_NAME=codex`
- or `export AGENT_NAME=antigravity`

Pre-commit auto checks already enabled:
- role check: `scripts/verify-agent-role.sh`
- lock check: `scripts/verify-agent-locks.sh`

File lock flow:
1. `scripts/agent-file-guard.sh claim <agent> <file...>`
2. `scripts/agent-file-guard.sh check <agent> <file...>`
3. edit/test/commit
4. `scripts/agent-file-guard.sh release <agent> <file...>`

## 5) Primary SOT Files
- `/Users/rathoreajitmsingh/Project/bookmy.bike/.agents/workflows/codex-audit-only-orchestration.md`
- `/Users/rathoreajitmsingh/Project/bookmy.bike/.agents/workflows/dual-agent-file-guard.md`
- `/Users/rathoreajitmsingh/Project/bookmy.bike/catalog_data_sot_notes.md`

## 6) Response Style (Codex)
- Explain in simple Hindi/hinglish.
- Give page-wise and file-wise clarity.
- Always mention assumptions and acceptance criteria.
- Keep implementation ownership with Antigravity unless override keyword is given.

## 7) New Chat Starter (copy this)
Bootstrap from `@codex_login`.
Follow audit-only mode for Codex and implementation-only mode for Antigravity.
Do not implement unless I write `CODEX_OVERRIDE_IMPLEMENT`.

